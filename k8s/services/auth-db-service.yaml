apiVersion: v1
kind: Service
metadata:
  labels:
    service: auth-db-service
  name: auth-db-service
  namespace: default
spec:
  clusterIP: None
  ports:
  - name: mysql
    port: 3306
    targetPort: 3306
  selector:
    app: auth-db
  type: ClusterIP