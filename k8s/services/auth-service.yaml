apiVersion: v1
kind: Service
metadata:
  labels:
    service: auth-service
  name: auth-service
  namespace: default
spec:
  ports:
  - name: chess-auth-service
    port: 8000
    targetPort: 8000
  selector:
    app: chess-auth-service
  type: ClusterIP