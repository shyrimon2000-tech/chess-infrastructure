apiVersion: v1
kind: Service
metadata:
  labels:
    service: game-db-service
  name: game-db-service
  namespace: default
spec:
  clusterIP: None
  ports:
  - name: mysql
    port: 3306
    targetPort: 3306
  selector:
    app: game-db
  type: ClusterIP