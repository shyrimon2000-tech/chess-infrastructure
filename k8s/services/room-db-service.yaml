apiVersion: v1
kind: Service
metadata:
  labels:
    service: room-db-service
  name: room-db-service
  namespace: default
spec:
  clusterIP: None
  ports:
  - name: mysql
    port: 3306
    targetPort: 3306
  selector:
    app: room-db
  type: ClusterIP