apiVersion: v1
kind: Service
metadata:
  labels:
    service: game-service
  name: game-service
  namespace: default
spec:
  ports:
  - name: chess-game-service
    port: 8000
    targetPort: 8000
  selector:
    app: chess-game-service
  type: ClusterIP