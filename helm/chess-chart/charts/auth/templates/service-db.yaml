{{- if .Values.db.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: auth-db-service
  labels:
    service: auth-db-service
spec:
  clusterIP: None
  ports:
  - name: mysql
    port: 3306
    targetPort: 3306
  selector:
    app: auth-db
{{- end }}