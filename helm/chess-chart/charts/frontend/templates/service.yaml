{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  labels:
    service: frontend-service
  name: frontend-service
spec:
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  selector:
    app: chess-frontend-service
  type: ClusterIP
{{- end }}