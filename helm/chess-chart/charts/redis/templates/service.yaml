{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  labels:
    service: redis-service
  name: redis-service
spec:
  clusterIP: None
  ports:
  - name: redis
    port: 6379
    targetPort: 6379
  selector:
    app: redis
  type: ClusterIP
{{- end }}