apiVersion: v1
kind: Service
metadata:
  name: game-db-service
  labels:
    service: game-db-service
spec:
  clusterIP: None
  ports:
  - name: mysql
    port: 3306
    targetPort: 3306
  selector:
    app: game-db
