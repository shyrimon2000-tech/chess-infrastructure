apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: game-networkpolicy
spec:
  podSelector:
    matchLabels:
      app: chess-game-service
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: ingress-nginx
      podSelector:
        matchLabels:
          app.kubernetes.io/name: ingress-nginx
    ports:
    - protocol: TCP
      port: 8000
  egress:
  {{- if .Values.db.enabled }}
  - to:
    - podSelector:
        matchLabels:
          app: game-db
    ports:
    - protocol: TCP
      port: 3306
  - to:
    - podSelector:
        matchLabels:
          app: redis
    ports:
    - protocol: TCP
      port: 6379
  {{- else }}
  - to:
    - ipBlock:
        cidr: {{ .Values.db.cidr }}
    ports:
    - protocol: TCP
      port: 3306
  - to:
    - ipBlock:
        cidr: {{ .Values.redisCidr }}
    ports:
    - protocol: TCP
      port: 6379
  {{- end }}
  - to:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: kube-system
      podSelector:
        matchLabels:
          k8s-app: kube-dns
    ports:
    - protocol: UDP
      port: 53
    - protocol: TCP
      port: 53
